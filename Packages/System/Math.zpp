fn abs(|T| n: T) -> T:
  return n if n >= 0 else -n

fn is_neg(|T| n: T) -> u8:
  return n < 0

fn max(|T| a: T, b: T) -> T:
  return a if a > b else b

fn min(|T| a: T, b: T) -> T:
  return a if a < b else b