-- 318

fn main(argc: u32, argv: **u8) -> i32:
  hash: u64 = argc as u64 - 1

  for .., argc > 1, argc -= 1:
    arg: *u8 = argv[argc - 1]

    i: u64 = 0
    for .., arg[i] != null, i += 1:
      hash += arg[i] as u64
    
    hash += i

  return hash as i32