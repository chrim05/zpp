fn add(a: u64, b: u64) -> u64:
  -- here the macro auto infers the arg types and ret type
  -- otherwise we need to specify them using generics
  --
  --                a    b   ret
  -- extern_call!(|u64, u64, u64| './math.o', 'c_add', a, b)
  return extern_call!('./math.o', 'c_add', a, b)

test 'add':
  .. = add(1, 2)