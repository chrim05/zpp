type Vec3 = (x: u64, y: u64, z: u64)

fn vec_add(a: *Vec3, b: *Vec3) -> Vec3:
  return (x: a.x + b.x, y: a.y + b.y, z: a.z + b.z)

fn main(argc: u32, argv: **u8) -> u8:
  v1: Vec3 = (x: 1, y: 3, z: 5)
  v2: Vec3 = (x: 2, y: 4, z: 6)
  v3: Vec3 = vec_add(&v1, &v2)

  return 0