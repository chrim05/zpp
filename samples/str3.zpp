-- 294

fn main(argc: u32, argv: **u8) -> i32:
  x: *u8 = "abc"

  -- casting each term to avoiding overflow
  -- if we do `(x[0] + x[1] + ..) as i32` we have UB
  return x[0] as i32 + x[1] as i32 + x[2] as i32
