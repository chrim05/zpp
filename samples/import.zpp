-- 39

from "module_to_import.zpp" import *

-- this function will be used by
-- `module_to_import.zpp` to test
-- recursive imports
fn add(a: i64, b: i64) -> i64:
  return a + b

fn main(argc: u32, argv: **u8) -> i32:
  a: Point = (x: 4, y: 20)
  b: Point = (x: 6, y: 9)

  c: Point = point_add(&a, &b)

  return (c.x + c.y) as i32