-- 0

fn main(argc: u32, argv: **u8) -> i32:
  x: f32 = 1.2
  y: i32 = (x + 0.8) as i32
  z: f64 = y as f64 + 0.1
  k: u64 = x as u64 + z as u64
  r: u8 = k == 3

  if x == 1.2:
    if y == 2:
      if z == 2.1:
        if r:
          return 0
  
  return 1